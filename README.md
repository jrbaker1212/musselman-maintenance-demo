# Musselman Maintenance — demo site (v3)

Static one-page preview for **Musselman Maintenance** (Jim). Phone-only contact: [(706) 571-8766](tel:+17065718766).

## Run locally

No build step. Serve the folder with any static server:

```bash
python3 -m http.server 8766
```

Open `http://127.0.0.1:8766/`.

## Files

- `index.html` — landing page
- `styles.css` — layout and typography
- `images/` — committed demo stock photography (no hotlinked CDN URLs)
- `favicon.svg`

## Preview ribbon

The top bar **Preview — not published** is intentional for cold-email demos.

## Handoff archive

`musselman-v3.tar.gz` at the repo root contains the deployable site files.
