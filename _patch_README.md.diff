--- a/README.md
+++ b/README.md
@@ -1,36 +1,40 @@
-# Musselman Maintenance — demo one-pager
+# Musselman Maintenance — sales demo site
 
-Static sales-demo website for **Musselman Maintenance** (plumbing / maintenance, Columbus, GA metro). This is a **preview only** — not a live production site.
+Premium static one-pager for **Musselman Maintenance** (plumbing & maintenance, Columbus GA metro / Phenix City AL / Cataula). **Preview only** — not published.
 
 ## Preview locally
 
-**Option 1 — open the file**
+**Quick open:** double-click `index.html` (hero imagery loads from Unsplash; use a server if your browser blocks remote fonts/images).
 
-Open `index.html` in your browser (double-click or drag into a window).
+**Recommended — local server:**
 
-**Option 2 — local server (recommended)**
-
-From this folder:
-
 ```bash
+cd /path/to/this/repo
 python3 -m http.server 8766
 ```
 
-Then visit [http://127.0.0.1:8766](http://127.0.0.1:8766).
+Open [http://127.0.0.1:8766](http://127.0.0.1:8766) in your browser.
 
 ## Contact on the demo
 
-- Phone (click-to-call): **(706) 571-8766**
-- No email or contact form on this preview — phone only by design.
+- **Phone (click-to-call everywhere):** [(706) 571-8766](tel:+17065718766)
+- Owner signal: Jim
+- No email or contact form — phone-only by design for this preview.
 
 ## Files
 
 | File | Purpose |
 |------|---------|
-| `index.html` | Single-page layout (hero, services, area, trust, footer) |
-| `styles.css` | Mobile-first styling |
-| `favicon.svg` | Tab icon |
+| `index.html` | Single-page layout: hero, services, CTA band, gallery, trust, area, footer |
+| `styles.css` | Typography, color system, responsive layout, CSS motion |
+| `favicon.svg` | Tab icon (navy + copper) |
 
+## Design notes
+
+- Stock photography via Unsplash URLs (requires network when previewing).
+- Testimonial block is **labeled placeholder copy** — replace with real quotes when going live.
+- Orange **Preview — not published** ribbon is fixed at the top.
+
 ## Offer context (internal)
 
-Cold-email demo positioning: $299 site · $349 go-live + host · $19/mo maintain.
+Cold-email demo positioning: **$299** site · **$349** go-live + host · **$19/mo** maintain.
